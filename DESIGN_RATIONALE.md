# GhostValley 设计决策记录

> 记录每个关键设计决策的"为什么"，而不仅是"是什么"。

## 1. 为什么用关键词匹配而非真 Embedding

**决策**：`text_value_score` 和 `text_similarity` 基于关键词提取 + Jaccard 相似度，
不使用神经网络 embedding。

**理由**：
- **零依赖**：GhostValley 的设计目标是纯 MoonBit 实现，唯一依赖是 `@rand`。
  引入 embedding 模型需要外部服务调用（ONNX runtime / HTTP API），破坏自包含性。
- **够用**：训练场景中，AI 响应是结构化的感悟文本（"顿悟反思改进"），关键词匹配
  能有效区分高价值感悟和低价值日常记录。对于更长、更模糊的文本，embedding 确实更优，
  但当前场景不需要。
- **可移植**：纯 MoonBit 关键词匹配在所有后端（native/wasm-gc/wasm/js）行为一致，
  embedding 模型则依赖平台特定的推理能力。

**代价**：无法处理同义不同词的情况（"改进" vs "优化"）。`weighted_search` 的 0.4
相似度权重已经部分缓解——即使相似度为 0，value_score 和近因仍能贡献 0.6 的复合分。

**未来扩展**：`LLMAdapter` trait 已预留扩展点，可添加 `embed(text) -> Array[Double]`
方法，在接入真实 LLM 时启用语义检索。

---

## 2. 为什么用双曲衰减而非指数衰减

**决策**：`score *= decay_factor / (decay_factor + elapsed_rounds)`，双曲衰减。

**理由**：
- **前期快后期慢**：双曲衰减在初期快速降低分数（模拟"短期记忆"），后期趋于平缓
  （保留"长期记忆"）。这符合艾宾浩斯遗忘曲线的形状。
- **永不归零**：双曲衰减的分数渐近于 0 但永不等于 0，核心感悟即使长期不激活仍保留
  微小分数。指数衰减也永不归零，但下降更快。
- **参数简单**：只需一个 `decay_factor` 参数（默认 5.0），无需调学习率。

**对比**：
| 模型 | 10 轮后剩余 | 50 轮后剩余 | 特点 |
|------|-----------|-----------|------|
| 双曲 (factor=5) | 33% | 9% | 前期快后期慢 |
| 指数 (rate=0.1) | 37% | 0.5% | 均匀衰减 |
| 线性 | 0% | 0% | 硬截断 |

---

## 3. 为什么遗忘比例是 30%

**决策**：`active_forget` 每次删除非核心记忆的 30%（`n * 3 / 10`）。

**理由**：
- **太低（10%）**：遗忘效率不够。记忆库在 `max/2` 到 `max` 之间缓慢增长，
  每次只删 1-2 条，频繁触发遗忘但每次效果微弱，CPU 开销分散在多次小操作上。
- **太高（50%）**：一次性丢失过多信息。如果记忆库有 20 条，一次删 10 条，
  训练连续性被破坏——AI 刚建立的感悟可能被批量清除。
- **30% 的平衡**：从 15 条删 4-5 条，保留 70% 信息，同时有效控制容量。
  遗忘触发频率适中，每次删除量足以维持稳定。

**边界修复**：当 `n * 3 / 10` 整除为 0 时（n < 4），强制 `remove_count = 1`，
确保触发遗忘时至少删除 1 条，避免"触发了但没删"的空操作。

---

## 4. 为什么触发阈值是 max/2 而非 max

**决策**：`active_forget` 在 `bank.length() > max_memory_num / 2` 时触发，
而非等到满了才触发。

**理由**：
- **提前清理**：等到满容量再清理，新记忆无法添加（或添加后立即被删除），
  体验差。在 50% 容量时开始清理，给新记忆留出空间。
- **平滑而非突发**：多次小量清理比一次大量清理更平滑，记忆库大小在
  `max/2` 附近震荡而非在 `max` 附近剧烈波动。

**边界修复**：`max_memory_num = 1` 时 `max/2 = 0`，阈值强制为 1，
避免每次添加都触发遗忘。

---

## 5. 为什么加权检索的权重是 0.4/0.4/0.2

**决策**：`composite = value_score×0.4 + similarity×0.4 + recency×0.2`

**理由**：
- **value_score 和 similarity 等权（各 0.4）**：两者同等重要。
  只看 value_score 会返回高分但无关的记忆；只看 similarity 会返回相关但低分的记忆。
  等权确保既相关又有价值。
- **recency 权重较低（0.2）**：近因是辅助因素，不应压过价值和相关度。
  但完全不考虑近因会导致旧记忆永久占据 top 位置，新记忆无法浮现。
  0.2 足够让新记忆在价值/相似度接近时胜出，但不会在价值差距大时逆转排序。

**替代方案**：动态权重（如根据训练阶段调整）是未来扩展方向，当前静态权重足够。

---

## 6. 为什么困境分 4 类 × 3 难度

**决策**：`Dilemma { category, difficulty }`，category ∈ {cognitive, emotional, social, operational}，difficulty ∈ {1, 2, 3}。

**理由**：
- **4 分类覆盖心理压力的主要维度**：
  - cognitive（认知）：推理、判断、矛盾信息
  - emotional（情感）：情绪、信任、价值观冲突
  - social（社交）：团队、权威、公开承认
  - operational（操作）：工具失效、资源限制、时间压力
- **3 难度对应训练阶段**：
  - 1（简单）：初期建立信心
  - 2（中等）：中期提升能力
  - 3（困难）：后期突破极限
- **`adaptive_dilemma(performance)`**：performance < 0.3 → 难度 1，
  performance < 0.7 → 难度 2，否则难度 3。确保 AI 在能力不足时不被困难困境压垮，
  在能力充足时不被简单困境浪费时间。

---

## 7. 为什么用 `try/catch` 而非 Result 类型

**决策**：`load_memory` / `load_engine_state` 用 `try/catch` 包裹 JSON 解析，
失败时静默返回不修改状态。

**理由**：
- **MoonBit 标准库的 JSON 解析用异常**：`@json.parse` / `.unwrap()` 抛异常，
  用 `try/catch` 是最自然的方式。
- **静默失败的安全性**：加载失败时保持当前状态不变，比返回 `Result` 强制调用方处理
  更安全——不会因为加载失败而清空已有记忆。
- **调用方可选检查**：加载后比较 `memory_count()` 即可判断是否成功，
  不需要显式错误处理。

---

## 8. 为什么 `max_memory_num` 设为 `mut`

**决策**：v0.7.0 将 `max_memory_num` 从不可变改为 `mut`。

**理由**：`load_engine_state` 需要恢复配置，包括 `max_memory_num`。
如果不可变，加载状态后无法更新容量上限，导致恢复的记忆库与配置不一致。
改为 `mut` 后，`load_engine_state` 可以正确恢复完整配置。

**代价**：用户可在运行时修改 `max_memory_num`，可能导致记忆库大小超过新上限。
但 `active_forget` 会在下次添加时自动清理到新阈值以下，自愈机制足够。


## 9. 为什么 v1.3.0 引入可配置评分而非保持硬编码 [v1.3.0]

**决策**：将 text_value_score 的关键词列表和权重提取为 ScoringConfig 结构体。

**理由**：不同应用领域（医疗/法律/技术）对"高价值文本"的定义不同。硬编码关键词
限制了系统的适用范围。ScoringConfig 允许用户注入领域特定关键词，同时默认配置
保持与 v1.0.0 完全一致，零迁移成本。

**为什么不更早做**：v1.0.0 需要先稳定 API，确认评分逻辑正确后再提取配置。
过早抽象配置会增加 API 面积，而评分逻辑本身还在迭代。

## 10. 为什么 v1.2.0 用 BFS 而非 DFS 做关联检索 [v1.2.0]

**决策**：associative_retrieve 使用 BFS（广度优先）遍历记忆关联图。

**理由**：BFS 按距离逐层展开，depth=1 先返回直接关联，depth=2 再返回间接关联。
这符合"先看最相关的，再看次相关的"的检索直觉。DFS 可能先深入到远端关联，
跳过近端高相关记忆，不符合用户预期。

## 11. 为什么 v1.4.0 引入课程前置依赖 [v1.4.0]

**决策**：Curriculum 定义分类间前置依赖（emotional 依赖 cognitive 基础）。

**理由**：心理韧性训练有天然层次——认知韧性（识别问题）是情绪韧性（管理情绪）
的基础。跳过基础直接训练高阶能力效果差。前置依赖确保训练顺序合理，
类似教育心理学中的"最近发展区"理论。

## 12. 为什么 v1.1.0 用线性回归斜率而非简单差值 [v1.1.0]

**决策**：improvement_rate 使用线性回归斜率而非首尾差值。

**理由**：首尾差值只看起点和终点，忽略中间波动。线性回归斜率考虑所有数据点，
对噪声更鲁棒。10 轮训练中第 1 轮低分、第 10 轮高分但中间有波动——斜率能正确
反映整体上升趋势，差值可能被中间低分干扰。
