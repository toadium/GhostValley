# MoonBit 黑客松参赛申报书 — GhostValley 幽灵谷工程

## 项目信息

- **项目名称**：GhostValley（幽灵谷工程）
- **项目仓库**：`https://github.com/toadium/GhostValley`
- **项目类型**：T2 移植项目（Python → MoonBit）
- **当前版本**：v1.0.0
- **许可证**：Apache-2.0
- **mooncakes 包**：`walkzzz/ghostvalley`（`moon add walkzzz/ghostvalley`）

## 项目简介

GhostValley 是一套 AI 心理压力演练系统，通过可控的模拟挫折与压力训练，
锤炼 AI 抗压自愈能力。AI 在自主经历困境中自我顿悟、迭代进化。

系统包含五大核心模块：温石验证、防创伤安全气垫、主动式遗忘机制、
第八根空桩、建造者退场逻辑。

## 技术亮点

### 1. 零依赖纯 MoonBit

唯一依赖 `@rand`，不引入任何外部服务或模型调用。
在 native/wasm-gc/wasm/js 四后端行为一致。

### 2. 加权综合检索

`weighted_search` 将 value_score（0.4）、text_similarity（0.4）、时间近因（0.2）
合成复合分排序，兼顾价值、相关性和新鲜度。

### 3. 自动化训练管道

`run_training_pipeline` 一行代码完成多轮训练：选困境 → LLM 响应 → 评分 → 存记忆 →
定期衰减 → 定期清理 → 记录统计。`TrainingStats` 追踪保留率、平均分趋势等跨轮次指标。

### 4. 结构化困境 + 自适应难度

`Dilemma { content, category, difficulty }` 支持 4 分类（cognitive/emotional/social/operational）
× 3 难度。`adaptive_dilemma(performance)` 按 AI 当前表现自动选择合适难度。

### 5. 全状态持久化

`save_engine_state` / `load_engine_state` 保存/恢复完整引擎状态
（memory + stats + config + rounds），支持跨会话训练恢复。

### 6. 性能优化

`top_memories` 从 O(n log n) 全排序优化为 O(n*k) partial selection，
在 n >> k 时显著更快。

## 测试覆盖

- **216 个测试**，四后端全通过
- **测试类型**：单元 + 边界 + 属性 + 模糊 + 基准
- **基准规模**：1000 条记忆下检索/遗忘/衰减/状态持久化

## 文档

- README.mbt.md — API 参考 + 完整使用教程
- ARCHITECTURE.md — 架构文档（五大模块/数据流/扩展点）
- DESIGN_RATIONALE.md — 8 条设计决策记录
- CHANGELOG.md — v0.1.0 → v1.0.0 完整变更日志

## 代码规模

- 源码：~2200 行
- 测试：~1730 行
- 公开 API：60+
- 结构体：AiMemory, Dilemma, TrainingLogEntry, TrainingLog, TrainingStats, EngineState, TrainingReport
- Trait：LLMAdapter


## v1.1.0 - v1.4.0 迭代功能

- **v1.1.0 训练评估**：韧性分、分类覆盖度、改善率、S/A/B/C/D 评级
- **v1.2.0 记忆关联图谱**：双向关联、BFS 关联检索、间隔重复强化、访问频率追踪
- **v1.3.0 可配置评分+事件系统**：自定义关键词/权重、三种衰减模型、事件回调
- **v1.4.0 训练课程体系**：前置依赖、课程进度追踪、课程驱动训练管道
- **测试数量**：300 个（三后端全通过）
