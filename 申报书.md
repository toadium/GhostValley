# GhostValley 幽灵谷工程 — 参赛申报书

## 一、项目定位

**GhostValley（幽灵谷工程）** 是一套 AI 心理压力演练系统，通过可控的模拟挫折与压力训练，锤炼 AI 抗压自愈能力，让 AI 在自主经历困境中自我顿悟、迭代进化。

本项目为 **Python 幽灵谷工程的 MoonBit 移植版**（T2 移植项目），100% MoonBit 实现，依赖标准库 `@rand`、`@debug`、`@json`。

## 二、核心功能 — 五大模块

| 模块 | 说明 | 技术实现 |
|------|------|---------|
| 温石验证 | 知情同意升级，靠态度文本打分，敷衍直接拒绝 | 关键词匹配 + 加权打分（愿意词 +0.25，敷衍词 -0.4） |
| 防创伤安全气垫 | 模拟挫折数据强制标记 `is_simulation` | `AiMemory.is_simulation` 字段，AI 知道这是模拟 |
| 主动式遗忘机制 | AI 自主打分，主动淘汰低价值记忆 | 超容量 50% 时触发，删除最低价值 30% 非核心记忆 |
| 第八根空桩 | 不内置标准答案，只给困境问题 | 4 条困境池随机选取，返回问题不返回答案 |
| 建造者退场逻辑 | 开发者不干预推理输出 | `accept_ai_self_gain` 接收 AI 自主感悟，不预设结论 |

## 三、扩展功能

| 功能 | 说明 |
|------|------|
| 记忆检索 | `search_memories`/`top_memories`/`filter_memories`/`average_value_score` |
| 记忆分类 | `real_memories`/`simulation_memories`/`core_memories` |
| 记忆衰减 | `apply_decay` 双曲衰减模型，高分记忆衰减更慢 |
| 低价值清理 | `cleanup_low_value` 按阈值清理非核心记忆 |
| 训练日志 | `TrainingLog`/`TrainingLogEntry` 结构化日志 + JSON 导出 |
| LLM 适配 | `trait LLMAdapter` + `MockLLM` 实现，支持自动训练 |
| 记忆持久化 | `save_memory`/`load_memory` JSON 序列化/反序列化 |
| 训练报告 | `TrainingReport` 结构化报告 + JSON 导出 |
| 配置化困境池 | `add_dilemma`/`set_dilemma_pool`/`dilemma_pool_size` 动态管理 |
| 语义打分 | `text_value_score`/`text_similarity` 基于关键词匹配 |

## 四、技术路线

- **语言**：MoonBit（100%）
- **依赖**：`moonbitlang/core/random`、`moonbitlang/core/debug`、`moonbitlang/core/json`
- **四后端兼容**：native / wasm-gc / wasm / js 全部通过
- **测试**：115 个测试四后端全通过
- **API 设计**：指令式引擎 API，`GhostValleyEngine` 封装全部状态
- **公开 API**：47 个公开函数

## 五、移植说明

| 项 | 信息 |
|----|------|
| 原项目 | 幽灵谷工程 Python 原版（用户提供概念性原型） |
| 移植范围 | 五大模块全部移植 + 记忆检索/衰减/日志/配置化扩展 |
| 修改说明 | `dataclass→struct`、`random→@rand`、`print→返回String`、动态类型→静态类型 |
| 未支持功能 | 对接真实 LLM API（当前为 MockLLM 模拟推理） |

## 六、工程规范

- ✅ Git 仓库：`https://github.com/toadium/GhostValley`
- ✅ mooncakes 发布：`walkzzz/ghostvalley v0.4.0`
- ✅ CI 配置：四后端 check + test + fmt
- ✅ LICENSE：Apache-2.0
- ✅ README：完整 API 参考 + 移植说明
- ✅ CHANGELOG：版本迭代记录
- ✅ 独立示例：`examples/demo/`
- ✅ 自查报告：`自查报告.md`

## 七、版本迭代历史

| 版本 | 主要内容 |
|------|---------|
| v0.1.0 | 初始移植，五大模块，21 测试 |
| v0.2.0 | Embedding 打分 + LLM 接口 + 持久化 + 训练报告，55 测试 |
| v0.3.0 | 记忆检索 + 衰减机制 + 训练日志 + 配置化困境池，110 测试 |
| v0.4.0 | 边界安全修复 + js 后端 CI，115 测试 |

## 八、对 MoonBit 生态的价值

MoonBit 生态中首个 AI 心理压力演练系统，展示了 MoonBit 在以下场景的适用性：
- AI 安全训练（防创伤标记）
- 受控模拟环境（安全气垫）
- 自适应遗忘机制（资源管理）
- 空桩设计模式（不预设答案的困境训练）
- 四后端兼容（native/wasm-gc/wasm/js）
